generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider  = "postgresql"
  url       = env("POSTGRES_DATABASE_URL") // pgbouncer connection
  directUrl = env("POSTGRES_DIRECT_URL") // direct postgres for migrations
}
