model ProjectQuizSubmission {
  id      String @id @default(uuid())
  stepId  String
  userId  String
  answers Json // key: questionId, value: selected answer
  score   Float?

  createdAt DateTime @default(now())
  updatedAt DateTime @updatedAt
}
